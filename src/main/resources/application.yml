server:
  port: 9011
spring:
  application:
    name: tourreservation-service
  datasource:
    url: jdbc:mysql://localhost:3306/destination?serverTimezone=Asia/Seoul
    username: root
    password: 1q2w3e4r!!
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  mvc:
    hiddenmethod:
      filter:
        enabled=true: